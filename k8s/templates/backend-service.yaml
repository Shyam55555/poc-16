apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: {{ .Values.backend.serviceType }}
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: {{ .Values.backend.port }}
